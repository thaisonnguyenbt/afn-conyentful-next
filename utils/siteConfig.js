module.exports = {
    siteUrl: `https://asianfoodnetwork.com`, // Site domain. Do not include a trailing slash!
    defaultLanguage: "en",
    defaultLocale: "en-US",
    allLocales: ["en-US", "th-TH"],
    allLanguages: ["en", "th"],
    defaultBrightcoveAccount : "4011286619001",
    postsPerPage: 9, // Number of posts shown on paginated pages (changes this requires sometimes to delete the cache)

    siteTitleMeta: `Asian Food Network | The Home Of Asian Recipes & Cuisine`, // This allows an alternative site title for meta data for pages.
    siteDescriptionMeta: `Asian Food Network (AFN) is the world's home of Asian Recipes & Cuisine. Discover authentic asian recipes, asian travel guides on Asian Food Network. Easy Chinese recipes, Malaysian food recipes, top Filipino food recipes.`, // This allows an alternative site description for meta data for pages.

    shareImageWidth: 1000, // Change to the width of your default share image
    shareImageHeight: 523, // Change to the height of your default share image

    shortTitle: `Asian Food Network`, // Used for App manifest e.g. Mobile Home Screen
    siteIcon: `favicon.png`, // Logo in /static dir used for SEO, RSS, and App manifest
    backgroundColor: `#e9e9e9`, // Used for Offline Manifest
    themeColor: `#15171A`, // Used for Offline Manifest
}
